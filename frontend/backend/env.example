APP_ENV=dev
DATABASE_URL=postgresql+psycopg2://forumkit:forumkit@db:5432/forumkit
JWT_SECRET_KEY=change-this-in-prod
PASSWORD_SALT_ROUNDS=12

# JWT Token 過期時間設定
JWT_ACCESS_TOKEN_EXPIRES_HOURS=168  # Access Token 過期時間（小時），預設 168 小時 = 7 天
JWT_REFRESH_TOKEN_EXPIRES_DAYS=30   # Refresh Token 過期時間（天），預設 30 天

# 通知 webhook（Discord 相容，用於 /api/support/report 等回報/建議）
ADMIN_NOTIFY_WEBHOOK=
